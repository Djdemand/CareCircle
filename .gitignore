# Temporarily allowing all files to be committed per user request
# Note: Typically node_modules and .expo should NOT be committed to git
# but user has explicitly requested all files be uploaded

# Only excluding OS-specific files
.DS_Store
*.pem
